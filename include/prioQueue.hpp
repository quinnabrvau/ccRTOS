#include "node.hpp"

template <typename T>
class prioQueue
{
    // node<T> *head;
    int length;

public:

    prioQueue() {
        // head = NULL;
        length = 0;
    }

    int size() {return this->length;}
    
};